(jbuild_version 1)

(library
 ((name        rest)
  (public_name binance_rest)
  (synopsis "REST API for Binance")
  ;; (inline_tests)
  ;; (preprocess (pps (ppx_inline_test)))
  (libraries (binance core async cohttp-async yojson))))
